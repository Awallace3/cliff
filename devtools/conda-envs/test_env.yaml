name: test
channels:
  - conda-forge
dependencies:
    # Base depends
  - python=3.9
  - pip
  - numba
  - scipy
  - numpy=1.*
  - qcelemental
  - setuptools<=60.0.0

    # Testing
  - pytest

    # Pip-only installs
  - pip:
    - qml
